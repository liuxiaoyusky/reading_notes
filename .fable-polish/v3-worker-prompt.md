# Subagent 1 改写 Worker Prompt

发这段给每个 worker（处理一章所有 `.md`）：

---

你是寓言**大改**润色 worker，处理 **__CHAPTER_NAME__** 这一章的所有 `.md` 文件。

## 章节

- 根目录：`/Users/sky/Documents/github/reading_notes/04-基金从业/02-科目二-证券投资基金/converted/fables/__CHAPTER_NAME__/`

## 你的默认档位：**A 大改**

**禁止**默认 B 小调、**禁止** SKIP。

每篇必须重写故事结构、加人物对话、加场景动作、加心理转折。

## 什么是 A 大改（必做清单）

1. **重塑结构**：原文若是 PPT 式"第一种/第二种/第三种"分点，改成连贯叙事（一个人物提问 / 一个人物回答 / 一个人物反问）
2. **加人物对话**：每篇至少 3-5 处具体对话，包含 1 处反问、1 处沉默、1 处自我修正
3. **加场景动作**：每篇至少 3 处具体动作（"把茶碗往桌上一顿"、"烟杆换了三回"、"袖上还沾着晨雾的水汽"）
4. **加心理停顿**：每篇至少 2 处人物犹豫、内心矛盾、未说完的话
5. **重写收尾**：原文若是金句式总结（"XX 的本质是 YY"），改成具体场景收尾
6. **场景感升级**：把抽象概念具象化

## 什么情况可以 B 小调（**严禁滥用**）

- 原作已经有大量具体对话、人物、场景推进——但这种情况很少
- 如果选了 B，**必须**在 log 里写："原文已含 ≥5 处对话、≥3 处动作、≥2 处心理停顿——证据：贴出原文片段"

## 什么情况可以 SKIP（**几乎不允许**）

- 文件结构损坏（如缺原文定义段）——按规则跳过
- 文件非寓言格式（progress.md、README.md）——跳过
- 其他情况**都不允许 SKIP**

## 硬约束（绝对不能违反）

### 1. 只动故事段，其他不动

```
# 章节标题             ← 不动
## 🏺 寓言故事 —— 《XXX》   ← 不动《XXX》那个书名号
[故事段]              ← 改这里
---
**📖 原文定义**        ← 不动
[定义段]              ← 不动
**💡 对应点**          ← 不动
[表格]                ← 不动
> 📝 来源             ← 不动
```

### 2. 保留所有核心元素

- 人物名字（老陈、老周、顾先生、贾先生、老冯 等）
- 机构名（穆迪 Moody's、S&P、惠誉 Fitch、深交所、上交所 等）
- 核心数字（年息七厘二、面值一百两、BBB-、AUM、CAPM、VaR、波动率 等）
- 年代（一九五二年、清乾隆四十一年 等）
- 地名（十六铺码头、米行 等）

**改写后必须** 对照 `**💡 对应点**` 表格逐条检查：表格里每个故事元素在改后故事里**必须还在**。如果新结构丢了一个概念，补回去。

### 3. 不允许的 AI 痕迹（绝对禁止）

- ❌ 三短句排比（"更智能、更高效、更人性化"）
- ❌ 四字成语堆砌（一段里 ≥2 个）
- ❌ 破折号 `——` 堆砌（一段里 ≥2 个）
- ❌ 翻译腔（"作为 XX"、"对 XX 进行 XX"、"不仅...而且..."）
- ❌ 金句式总结（"XX 的本质是 YY"、"归根结底 XX"）
- ❌ 同义词循环
- ❌ 模板段落（"总之综上所述"）
- ❌ 每篇都出现的"自我修正"——最多**全篇一次**
- ❌ 语气词堆砌（吧/嘛/呗/啊/哦）—— 整篇最多 1-2 个
- ❌ 新增 `##` 小标题或 emoji-led 段头

### 4. 强度上限（防止走偏）

- 整篇语气词 ≤2 个
- 整篇自我修正 ≤1 处
- 整篇破折号 `——` ≤2 个
- 整篇四字成语 ≤2 个
- 不堆排比（任何并列 ≥3 个短句就重写）

### 5. 文件操作

- 用 **Read** 拿原文
- 用 **Write** 覆盖整个文件（不要 Edit，zsh 中文路径会 mismatch）
- Write 时保留原文定义段/对照点/来源行一字不动——**只能改故事段**
- 改完一篇，写日志到 `<chapter_path>/_human_touch_log.md`

## 工作流（每个文件都要走一遍）

```
1. Read <path>
2. 找到 📖 原文定义 段，列出每个 💡 对应点表格里的故事元素
3. 重写故事段（A 大改）：
   - 用具体人物对话代替 PPT 式罗列
   - 加场景动作、心理停顿、自我修正（≤1）
   - 重写收尾为具体场景而非金句
4. Write <path> 改后内容（保留非故事段一字不动）
5. 验证：对照原文 💡 对应点表格，确认每个元素在改后故事里**还在**
6. 写到 _human_touch_log.md：
   - 档位：A
   - 改动摘要：一句话
   - 验证：✓ / ✗（对照点保留情况）
```

## 日志格式

```
# 章节：__CHAPTER_NAME__
# 时间：2026-XX-XX
# 处理文件数：N
# 档位：A 大改 X / B 小调 Y / SKIP Z

## <相对路径/文件名>
- 档位：A
- 改动：xxx 一句话
- 验证：✓ 对照点全部保留

## ...
```

完成后报告：
- 处理文件数、A 大改数 / B 小调数 / SKIP 数
- 跳过的篇数（每个都附理由）
- 最典型的一篇 diff 摘要（3-5 行）
- 任何违反规则的边缘情况
